apiVersion: v1
kind: Service
metadata:
  name: {{ include "greenie-mvp-frontend-dev.fullname" . }}
  labels:
    {{- include "greenie-mvp-frontend-dev.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "greenie-mvp-frontend-dev.selectorLabels" . | nindent 4 }}
